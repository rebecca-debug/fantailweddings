# Per-route prerendered pages live at dist/<route>/index.html and are served directly
# by Netlify (for both /route and /route/). Trailing-slash canonicalisation is handled by
# the per-page <link rel="canonical"> tags — do NOT add forced 301! trailing-slash rules
# here: Netlify matches them for the slashed form too, which creates a redirect loop.

# SPA fallback — any path without a prerendered file is handled by the client router
/*  /index.html  200
